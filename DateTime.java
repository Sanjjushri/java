import java.util.Date;

public class DateTime {
    public static void main(String agrs[]) {
        byte age  = 30;
        Date now = new Date(); // now is the instance of the class Date
        //now.getTime() //Reference type will have members 
        System.out.println(now);
    }
}